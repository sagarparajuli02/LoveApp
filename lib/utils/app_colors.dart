import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Colors.pink;
  static final backgroundColor = Colors.pink[50];

  static const romanticGradient = LinearGradient(
    colors: [Color(0xFF831843), Color(0xFF3b0764)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );
}
